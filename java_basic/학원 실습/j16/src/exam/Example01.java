package exam;

public class Example01 {
	/*
	 * 인터페이스(Interface) - 클래스가 아님 -> 스스로 객체 못 만듬
	 * - 인터페이스는 추상메소드와 상수로만 이뤄져 있으며, 추상클래스와 마찬가지로
	 *   스스로 객체를 생성할 수 없다.
	 * - 인테페이스는 객체가 아니므로 생성자를 만들 수 없다
	 * - 다른 클래스 코드들과의 사이에서 중간 매개 역할을 한다
	 * - 다른 클래스들과 매개체 역할을 하기 때문에 누구나 접근할 수 있어야 한다 -> 항상 public이 붙어나감(자동)
	 * - 만약 접근제한자를 생략한다면 자동으로 public으로 인식함
	 * 필드) public, static, final
	 * 메소드) public, abstract
	 */
}
